# ===== 智能分诊系统依赖 =====
# 安装：pip install -r requirements.txt
# 版本与 conda 环境 xiangmu-2 对齐（Python 3.11）

# ---- LLM / LangGraph ----
langchain==0.3.25
langchain-core==0.3.86
langchain-openai==0.2.14
langchain-chroma==0.1.4
langgraph==0.2.74

# ---- 向量库 ----
chromadb==0.5.23

# ---- Web 服务 ----
fastapi==0.141.1
uvicorn==0.52.3
gradio==5.32.1
requests==2.32.3

# ---- MCP（Model Context Protocol）----
mcp==1.29.0            # 官方 MCP SDK（mcp_client：ClientSession / stdio_client）
fastmcp==3.4.7         # FastMCP 框架（mcp_server / hospital_mcp_server：FastMCP）

# ---- 配置 ----
pydantic==2.13.4
pydantic-settings==2.9.1

# ---- PostgreSQL ----
psycopg==3.2.9         # PostgresSaver / PostgresStore 驱动（v3）
psycopg-pool==3.2.6    # ConnectionPool
psycopg2==2.9.10       # 仅用于 import OperationalError 捕获数据库连接错误

# ---- 工具 ----
tenacity==9.1.2
concurrent-log-handler==0.9.25

# ---- 混合检索（BM25 + 中文分词）----
rank-bm25==0.2.2
jieba==0.42.1

# ---- 重排（可选：本地 BGE-reranker，CPU 推理慢，默认关闭）----
# 需要时手动安装（CPU 版 torch，避免拉取 2GB+ CUDA 包）：
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers==5.7.0
